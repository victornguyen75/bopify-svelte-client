export declare let cookieKeys: {
    code_verifier: string;
    access_token: string;
    refresh_token: string;
    expires_in: string;
};
export declare let fallbackValues: {
    scopes: string[];
    state: string;
};
